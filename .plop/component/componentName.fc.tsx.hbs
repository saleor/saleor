{{#if styled}}
import {
  createStyles,
  Theme,
  withStyles,
  WithStyles
} from "@material-ui/core/styles";
{{/if}}
import * as React from "react";

export interface {{ properCase name }}Props {
  
}

{{#if styled}}
const styles = (theme: Theme) => createStyles({
    
});
const {{ properCase name }} = withStyles(styles, { name: "{{ properCase name }}" })(
  ({ classes }: {{ properCase name }}Props & WithStyles<typeof styles>) => <div />
);
{{else}}
const {{ properCase name }}: React.StatelessComponent<{{ properCase name }}Props> = () => <div />;
{{/if}}
{{ properCase name }}.displayName = "{{ properCase name }}";
export default {{ properCase name }};
