{{#if styled}}
import {
  createStyles,
  Theme,
  withStyles,
  WithStyles
} from "@material-ui/core/styles";
{{/if}}
import * as React from "react";

export interface {{ name }}Props {
  
}
interface {{ name }}State {
  
}

{{#if styled}}
const styles = (theme: Theme) => createStyles({
    
});
const {{ name }} = withStyles(styles, { name: "{{ name }}" })(
  class Inner{{ name }} extends React.Component<{{ name }}Props, {{ name }}State> {
    state: {{ name }}State = {

    }

    render() {

    }
  }
);
{{else}}
class {{ name }} extends React.Component<{{ name }}Props, {{ name }}State> {
  state: {{ name }}State = {

  }

  render() {

  }
}
{{/if}}
{{ name }}.displayName = "{{ name }}";
export default {{ name }};
