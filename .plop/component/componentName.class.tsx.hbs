{{#if styled}}
import {
  createStyles,
  Theme,
  withStyles,
  WithStyles
} from "@material-ui/core/styles";
{{/if}}
import * as React from "react";

export interface {{ properCase name }}Props {
  
}
interface {{ properCase name }}State {
  
}

{{#if styled}}
const styles = (theme: Theme) => createStyles({
    
});
const {{ properCase name }} = withStyles(styles, { name: "{{ properCase name }}" })(
  class Inner{{ properCase name }} extends React.Component<{{ properCase name }}Props, {{ properCase name }}State> {
    state: {{ properCase name }}State = {

    }

    render() {

    }
  }
);
{{else}}
class {{ properCase name }} extends React.Component<{{ properCase name }}Props, {{ properCase name }}State> {
  state: {{ properCase name }}State = {

  }

  render() {

  }
}
{{/if}}
{{ properCase name }}.displayName = "{{ properCase name }}";
export default {{ properCase name }};
