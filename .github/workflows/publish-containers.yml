name: Publish

on:
  workflow_call:
    inputs:
      ref:
        description: Optional ref name for custom build
        required: false
        type: string
      prefix:
        description: Optional image prefix for branch build
        required: false
        type: string
      version:
        description: Optional custom image version tag
        required: false
        type: string
      publish-environment:
        description: GitHub Environment name used for OIDC and secrets for build-push phase.
        required: true
        type: string
      notification-environment:
        description: The secrets for sending Slack notifications about the release.
        required: true
        type: string
    secrets:
      SLACK_WEBHOOK_URL:
      SLACK_MENTION_GROUP_ID:
    outputs:
      version:
        description: Docker image version
        value: ${{ jobs.prepare-variables.outputs.version }}

permissions: {}

jobs:
  prepare-variables:
    name: Prepare variables
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    outputs:
      image_name: ${{ steps.get-image-name.outputs.image_name }}
      labels: ${{ steps.metadata.outputs.labels }}
      tags: ${{ steps.metadata.outputs.tags }}
      version: ${{ steps.metadata.outputs.version }}
    steps:
      - name: Get GHCR image name (owner/repo)
        id: get-image-name
        env:
          GH_REPO: ${{ github.repository }}
        run: |
          # github.repository is already in owner/repo format required
          # by GHCR; normalize it to lowercase.
          GHCR_IMAGE_NAME="${GH_REPO,,}"
          echo "image_name=$GHCR_IMAGE_NAME" >> $GITHUB_OUTPUT

      # Required by docker/metadata-action
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}

      - name: Check if tag should be tagged as "latest"
        id: check-latest
        env:
          GH_TOKEN: ${{ github.token }}
          CURRENT_TAG: ${{ inputs.ref || github.ref_name }}
        run: |
          set -ux

          # Only stable semver releases (e.g. 3.22.0) can be tagged as latest.
          # Non-semver refs (branch names, PR refs) are not valid candidates to become latest.
          if ! [[ "$CURRENT_TAG" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Tag inferred from this run ('$CURRENT_TAG') is not a semver release - can't be tagged as latest."
            echo "IS_LATEST=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          CURRENT_LATEST=$(gh api "/repos/${GITHUB_REPOSITORY}/releases/latest" --jq .tag_name)
          HIGHEST=$( (echo "$CURRENT_LATEST"; echo "$CURRENT_TAG") | sort -V | tail -1)

          echo "Selected: $CURRENT_TAG, Latest: $CURRENT_LATEST"

          if [[ "$CURRENT_TAG" == "$HIGHEST" ]]; then
            echo "IS_LATEST=true" >> $GITHUB_OUTPUT
          else
            echo "IS_LATEST=false" >> $GITHUB_OUTPUT
          fi

      - name: Docker metadata
        id: metadata
        uses: docker/metadata-action@030e881283bb7a6894de51c315a6bfe6a94e05cf # v6.0.0
        with:
          images: |
            ghcr.io/${{ steps.get-image-name.outputs.image_name }}
          flavor: |
            latest=false
          tags: |
            type=ref,event=branch,prefix=${{ inputs.prefix }}
            type=pep440,pattern={{version}}
            type=pep440,pattern={{major}}.{{minor}}
            type=raw,value=${{ inputs.version }},enable=${{ inputs.version != null }}
            type=raw,value=latest,enable=${{ steps.check-latest.outputs.IS_LATEST }}
          context: git

  build-push:
    needs: prepare-variables
    uses: saleor/saleor-internal-actions/.github/workflows/build-push-image-multi-platform.yaml@49a069ae9731cfccf3a1033fe1da4e3da84f4f2a # v1.11.0
    permissions:
      contents: read
      id-token: write # needed for AWS/ECR login (not used, but required permission)
      packages: write # needed for GHCR
    with:
      checkout-ref: ${{ inputs.ref }}
      publish-environment: ${{ inputs.publish-environment }}

      enable-ghcr: true
      oci-full-repository: ghcr.io/${{ needs.prepare-variables.outputs.image_name }}
      tags: ${{ needs.prepare-variables.outputs.tags }}
      labels: ${{ needs.prepare-variables.outputs.labels }}

      amd64-runner-image: ubuntu-24.04
      arm64-runner-image: ubuntu-24.04-arm

  notify:
    if: ${{ failure() }}
    needs: [prepare-variables, build-push]
    uses: saleor/saleor-internal-actions/.github/workflows/notify-slack.yaml@49a069ae9731cfccf3a1033fe1da4e3da84f4f2a # v1.11.0
    with:
      type: build
      ref: ${{ needs.prepare-variables.outputs.version || inputs.ref || github.ref_name }}
      status: failure
      github-environment: ${{ inputs.notification-environment }}
    # Secrets used:
    # - SLACK_WEBHOOK_URL
    # - MENTION_GROUP_ID
    secrets: inherit # needed due to using environments
