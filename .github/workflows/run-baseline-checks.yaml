name: Run Baseline Checks
on:
  pull_request:
  schedule:
    - cron: "22 13 * * mon" # Every Monday at 13:22

jobs:
  run-all:
    uses: saleor/saleor-internal-actions/.github/workflows/run-baseline-checks.yaml@feat/add-baseline-checks
