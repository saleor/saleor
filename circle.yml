machine:
  node:
    version: "6.1.0"
  python:
    version: "3.5.1"
  post:
    - "pyenv global 2.7.11 3.4.4 3.5.1"
test:
  pre:
    - "npm run build-assets"
    - "./manage.py collectstatic --no-input"
  override:
    - tox
