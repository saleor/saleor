# Generated by Django 3.1 on 2021-01-07 12:07

from django.db import migrations

import saleor.core.db.fields
import saleor.core.sanitizers.editorjs_sanitizer


def clean_content_field(apps, schema_editor):
    Page = apps.get_model("page", "Page")
    PageTranslation = apps.get_model("page", "PageTranslation")

    models = [Page, PageTranslation]

    for model in models:

        for item in model.objects.iterator():
            item.content = "{}"
            item.save(update_fields=["content"])


def migrate_content_json_into_content_field(apps, schema_editor):
    Page = apps.get_model("page", "Page")
    PageTranslation = apps.get_model("page", "PageTranslation")

    models = [Page, PageTranslation]

    for model in models:

        for item in model.objects.iterator():
            item.content = item.content_json
            item.save(update_fields=["content"])


class Migration(migrations.Migration):

    dependencies = [
        ("page", "0017_pagetype"),
    ]

    operations = [
        migrations.RunPython(clean_content_field, migrations.RunPython.noop,),
        migrations.AlterField(
            model_name="page",
            name="content",
            field=saleor.core.db.fields.SanitizedJSONField(
                blank=True,
                default=dict,
                sanitizer=saleor.core.sanitizers.editorjs_sanitizer.clean_editor_js,
            ),
        ),
        migrations.AlterField(
            model_name="pagetranslation",
            name="content",
            field=saleor.core.db.fields.SanitizedJSONField(
                blank=True,
                default=dict,
                sanitizer=saleor.core.sanitizers.editorjs_sanitizer.clean_editor_js,
            ),
        ),
        migrations.RunPython(
            migrate_content_json_into_content_field, migrations.RunPython.noop,
        ),
    ]
